# CHERI_DTRACE_MALTA64 -- 64-bit kernel config for MALTA boards + 256-bit CHERI
#
# When using this kernel configuration, ensure that you are fine with linking
# against CDDL code.
#
# This is a modified version of the CHERI_MALTA64 that imports DTrace into the
# kernel by default.
#
# $FreeBSD$
 
include         CHERI_MALTA64
ident           CHERI_DTRACE_MALTA64

options         KDTRACE_HOOKS
options         KDTRACE_FRAME

device          dtrace
device          dtraceall

