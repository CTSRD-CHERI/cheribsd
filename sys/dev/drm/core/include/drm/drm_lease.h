/* Public domain. */

#ifndef DRM_LEASE_H
#define DRM_LEASE_H

#define drm_lease_held(f, id)		(true)
#define _drm_lease_held(f, id)		(true)
#define drm_lease_filter_crtcs(f, in)	(in)
#define drm_lease_revoke(m)
#define drm_lease_destroy(m)

#endif
