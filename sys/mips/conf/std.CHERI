#
# std.CHERI -- Convert a BERI kernel to a CHERI kernel by including
# this file.
#
# $FreeBSD$
#
nomakeoptions	ARCH_FLAGS
makeoptions	ARCH_FLAGS="-target cheri-unknown-freebsd -march=mips4 -mabi=64 -mcpu=mips4 -cheri=256"
options 	CPU_CHERI
options 	COMPAT_CHERIABI

options 	TMPFS
